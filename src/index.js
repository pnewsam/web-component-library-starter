// Import polyfills first to ensure any code dependent on them does not error.
import "@babel/polyfill";
import "@webcomponents/webcomponentsjs";

// Import all components here.
import "./components/x-icon";
